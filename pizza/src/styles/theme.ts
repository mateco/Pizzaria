import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: { main: '#FFB800' }, // Amarelo correto
    secondary: { main: '#FF5000' }, // Laranja/Vermelho
    success: { main: '#25D366'},  // Verde WhatsApp
    background: { default: '#D0D5DD' }, // Branco
  },
});
